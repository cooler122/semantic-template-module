package com.cooler.semantic.service.internal;

import com.cooler.semantic.entity.Account;

public interface AccountService extends BaseService<Account>{
    /**
     * 如果想用默认的数据库自动生成的所有api，则继承；
     * 如果选择用，则不继承并将选择的接口复制过来；（一般用这个，默认的还是会用到几个）
     * 如果不用，则不继承，接口自己写；
     */


}
